# plan-newformat.md — 레슨 신양식 (지시서㉑, 2026-08-08)

지시서: `작업지시서21-신양식-렌더러-원고전면개정.md` (대표 확정, 2026-08-08).
정본: `sample-신구성-3코스.json` + 지시서 §1-1 조립 스케치.

> 이 문서는 지시서가 구현자에게 위임한 결정(통 분할 등)과 그 근거,
> 실제 변경 스니펫, 트레이드오프를 기록한다. 지시서 자체가 승인된
> 계획이므로 이 문서는 **작업 이력**이다.

## 0. 리서치 요약 (현행)

| 물건 | 현행 |
|---|---|
| `renderDay` | 2통: ①`📚 N日目`+`【今日の文法】`+tip 원문+회화 / ②`🔖 今日の単語`+평면 목록 |
| bridge | `push-daily.fortuneSection(u, tpl)` → `fortuneMessage(f, lines, {bridge})` → 운세 말미 `──────────`+kr+ja |
| 퀴즈 | 3의 배수 아침에 `pickReviewQuiz`(과거 무작위 1문, action=review·무보존) / 절목 30·50·75 `tpl.quiz`(action=quiz·기록) |
| quickReply | **묶음의 마지막 1통에서만 열린다** (push-daily 주석·LINE 사양) — 절목 퀴즈가 항상 말미에 오는 이유 |
| 아침 묶음 | [1통, 2통, 운세, 부적, (예고), (복습/절목퀴즈)] · LINE 상한 5통, 부적은 `length<5`일 때만 |
| 신양식 판정 | (§2 지시) `vocab_3` 항목에 `pos`가 하나라도 있으면 신양식 |

## 1. 지시서가 위임한 결정 (§1-1 "구현자가 정하고 완료 보고에 명시")

### 1-1. 통 분할 — ❓+🍀는 꼬리통, 아침 묶음의 맨 끝

```
1통  📘 Day N : 문법  / [일본어부] / 🔗 接続 / 💡 学習ポイント / 💬 회화 3행
2통  📚 今日の単語 (품사 2·2·2)
꼬리통  ❓ 今日のクイズ + ①②③ + 🍀 今日のひとこと + quickReply(①②③)
```

push-daily 가 꼬리통을 뽑아 **운세·부적·예고 뒤(묶음 맨 끝)** 에 다시 붙인다.

**이유.** quickReply 는 묶음의 마지막 1통에서만 열린다(기존 절목 퀴즈가 말미로
가는 이유와 동일 — push-daily 주석). ❓를 2통 안에 넣으면 운세가 뒤에 오는 순간
답 버튼이 화면에 안 나온다. "quickReply 버튼은 기존 quizMessage 방식 그대로"를
지키려면 퀴즈를 나르는 통이 말미여야 한다. 🍀가 ❓ 뒤(스케치 순서)이므로 같은
통에 태운다 — 부수 효과로 bridge 는 이동 전과 같은 "아침의 맨 끝" 위치를 되찾는다.

- 1통·2통은 현행 분할 그대로(문법·회화 / 단어).
- 구양식(꼬리통 없음)에서 bridge 는 **레슨 마지막 통(2통) 말미**에 붙는다.

### 1-2. 데일리 ❓는 신양식 원고에서만

구양식 원고(현재 시드된 303일 전부)는 quiz 가 있어도 데일리 ❓를 만들지 않는다.
현행 거동(3의 배수 복습·절목만) 유지.

**이유.** 지시서 §6-1 이 배포① 시점의 화면 변화를 "헤더가 붙고 bridge 위치
이동"으로 못박았다. 구양식에도 ❓를 붙이면 세 번째 변화가 생긴다. §1-2 표의
"quiz 없음 → 퀴즈 섹션 생략 (현행과 동일)"도 같은 방향.

- 신양식 아침: 데일리 ❓가 붙고, 3의 배수 아침에는 ❓를 접고 🔁 복습만
  （A안 2026-08-10 — plan-quiz-review-on-multiple. 퀴즈 2건 금지는 유지）.
- 절목(30/50/75) 아침: 데일리 ❓를 접고(🍀는 2통으로) 절목 퀴즈가 말미.
  기록(action=quiz)이 무보존(action=review)에 밀리면 안 되기 때문.
- 데일리 ❓의 postback 은 **기존 review 방(무보존·즉시 채점)** 을 그대로 쓴다.
  `action=review&day=N` — 새 방을 만들지 않는다.

### 1-3. 통수 상한 (LINE 5통)

| 아침 | 구성 | 통수 |
|---|---|---|
| 신양식 보통 | 1·2통+운세+부적+❓ | 5 |
| 신양식 3배수 | 1·2통(🍀)+운세+부적+🔁 | 5 |
| 신양식+예고 | 1·2통+운세+예고+❓ (부적 쉼) | 5 |
| 신양식 절목 | 1·2통(🍀)+운세+부적+절목 | 5 |
| 절목+예고 | 1·2통(🍀)+운세+예고+절목 (부적 쉼, 현행 동일) | 5 |
| LINE 직접(운세 없음) | 1·2통+❓ | 3 |

부적의 `<5` 판정은 꼬리통을 셈에 넣는다(`messages.length + (quizTail?1:0) < 5`).

### 1-4. 문면 세부 (예시문 부재 시 스케치 직역)

- 대화 행: `{who}：{kr}` + 다음 행 `  （{ja}）` (2칸 들여쓰기·전각 괄호).
  구양식도 동일 적용 — 대화는 양식 공통 필드라 §1-2 표의 분기 대상이 아니다.
- 단어 행: `【名詞】 1. {kr}　{meaning}　2. …` (품사당 1행,
  `【名詞】`·`【動詞】` 뒤 반각 공백 1 — 스케치의 폭 맞춤 그대로).
- ①②③는 현행 quizMessage 그대로 **행마다 1개** (스케치의 1행 나열은 축약 표기로 읽음).
- `grammar_point` 분해: 말미 전각 `（…）`를 일본어부로. 없으면 `[…]` 행 생략.
- tip 분해: 행머리 `形`/`使`/`落`(+공백)으로 버킷 분류, 접두어는 떼고 출력.
  이어지는 무표지 행은 직전 버킷에 붙는다. 3버킷이 다 없으면 구양식으로 간주.

### 1-4b. 단어 뜻은 일본어 하나만 (한자 괄호 없음, 대표 2026-08-08)

`記事〔記事〕`처럼 같은 한자가 두 번 나오는 군더더기를 없앤다 —
**단어 뜻(`vocab_3[].meaning`)에서 `〔…〕`를 전부 제거**해 `記事` 하나만 낸다
(beginner-01-15 스타일과 동일). 지우개는 `_build-lib.mjs`의 `stripGloss`가
**vocab 뜻에만** 대므로, 대화의 지문 `〔면접에서〕`·`〔상자를 열며〕`는 그대로 남는다.
집필자는 소스에 어원 주석(`伝える〔傳-〕`)을 달아 둬도 되지만 출력엔 나오지 않는다.

- 적용 범위: 실물에서 `〔〕`가 있던 것은 **상급(신양식) 뿐**이었다(초·중급은 원래
  괄호 없음). 재생성으로 상급 101일 전량 정리, `seed-content --check` 303일 0건.

### 1-4c. tip 은 일본어로 (지시서 §0-2 · 2026-08-08 지적으로 시정)

지시서 §0-2 는 「tip 은 일본어 **形/使/落 3행 정형**(한국어 예시 형태만 한국어)」이고,
정본 샘플의 tip 도 일본어다. **초·중급 재작성 시 설명문을 한국어로 쓴 것은 위반**이라
202일치 tip 을 전량 일본어로 다시 썼다 (상급은 처음부터 일본어라 무변경).

- **形** 행은 활용 규칙이라 `語幹 + 겠어요` 처럼 한자어 + 한국어 형태만으로도 성립한다
  (상급도 같은 모양). **使·落 행은 설명문이므로 반드시 일본어**.
- 한국어가 남는 자리는 **예시 형태뿐** — 「안 가요（行かない）／못 가요（行けない）」처럼
  괄호로 일본어 뜻을 붙였다.
- 실측: 使·落 행 일본어 **101/101 · 101/101 · 101/101** (초·중·상).

### 1-5. pos 표기는 한 곳 (§2-3)

`render.mjs` 의 `POS = ["名詞","形容詞","動詞"]` 가 유일한 정의.
분류 키(원고의 `pos` 값)이자 표시 헤더(`【名詞】`)로 코드가 쓴다.
content-check 는 이것을 import 한다.

### 1-6. usableQuiz 이동

퀴즈 형 검사는 `render.mjs` 로 옮기고 `repo/learning.mjs` 가 재수출한다
(기존 `learning.usableQuiz` 호출부 무변경). 렌더러가 꼬리통을 만들 때
같은 판정이 필요한데, render→repo 방향의 import 는 층을 거꾸로 타기 때문.

## 1-7. 병행 계획서와의 대조 ([plan-lesson-format-v2.md](plan-lesson-format-v2.md))

같은 지시서를 조사만 한 계획서가 별도 세션에서 작성돼 있었다 (2026-08-08 12:09,
코드 무변경·정본 샘플 미입수 상태). 그 문서의 미해결 6건을 이 구현이 어떻게
처리했는지 대조해 둔다:

| 그쪽 § | 사안 | 이 구현의 처리 |
|---|---|---|
| A (3-2) | 🍀 수신자 확대 — 운세 못 받는 사람에게도 bridge 가 감 | **지시서 문면 그대로(전원)**. §6-1 이 배포① 변화로 「bridge 위치 이동」을 명시 — 구양식에서 숨기면 그 문장이 거짓이 된다. 이행기의 「学習運がよい」 노출은 §3 재작성이 끝날 때까지의 시한부. 완료 보고에 명기 |
| B (3-3-2) | 3의 배수 복습 퀴즈 존폐 | **존치**. 신양식 아침엔 데일리 ❓가 있으므로 뽑기를 쉼 — 아침당 퀴즈 1건 불변식으로 해소. 폐지 결정 불요 |
| C (3-3-1·3) | 절목 중복·LINE 5통 초과 | **해소**. 절목 아침·예고 아침은 데일리 ❓를 접는다(quizSection:false). 전 조합 5통 이내 — §1-3 표 |
| D (3-4-d) | 저녁 복습의 신양식 대응 | **범위 밖**(지시서에 없음). 신양식 도입 후 저녁 답변에 tip 3행·단어 6개가 원문대로 나감 — 깨지진 않고 스타일만 미적용. 완료 보고에 명기 |
| E (4-2) | 「이 상 없음」 군집이 실은 5개(33·34·37·39·41) | §3-2 후보 보고에 병합 — 교체 수는 지시서대로 2개(39·41), 33·34는 대표 판단 항목으로 병기 |
| F (4-1) | 어간 정규화 중복 검사 추가 | **범위 밖**으로 유지(diff 최소화). 후보 보고에 재발 방지책으로 언급만 |

vocab 개수 완화(§0-4 「3~6」)는 그쪽 3-4-b 의 지적대로 **구=정확히 3 / 신=정확히 6**
으로 구현한다 — 수용 집합 {3,6} ⊂ [3,6] 이므로 통과해야 할 원고는 전부 통과하고,
4·5개짜리 반쪽 원고만 추가로 걸린다.

## 2. 변경 파일

| 파일 | 내용 |
|---|---|
| `server/lib/render.mjs` | 신양식 조립·구양식 호환·꼬리통·`isNewFormat`/`parseTip`/`POS`/`usableQuiz` |
| `server/lib/fortune-text.mjs` | `fortuneMessage` 의 bridge 절 제거 (§1-3) |
| `server/db/push-daily.mjs` | `fortuneSection` 시그니처(template 제거)·꼬리통 말미 재배치·복습 뽑기 조건·부적 상한 셈 |
| `server/lib/repo/learning.mjs` | `usableQuiz` 재수출 |
| `server/lib/content-check.mjs` | §2 신양식 7규칙 (pos 판정·조건부) |
| `tools/verify-render.mjs` | §4 항목 + 신양식 픽스처 |
| `tools/verify-push.mjs` | fortuneSection 시그니처·bridge 이중 출력 부재·꼬리통 말미 |
| `tools/verify-fortune-server.mjs` | bridge 가 운세 메시지에 없음 (소스·출력) |
| `tools/verify-quiz.mjs` | 신양식 quiz 누락 거부·데일리 ❓와 복습 뽑기 배타 |
| `server/content/_build-lib.mjs` (로컬·비공개) | §3-1 공통: 검증·오답 풀 자동 구성·기계 검사 |
| `server/content/_build-advanced.mjs` (로컬·비공개) | 신양식 101일 전면 재작성 + §3-2 교체 |

## 3. 제외 (scope 밖)

- 저녁 복습(`renderReview*`)·절목/복습 퀴즈 문면 — 지시서에 없음. 무변경.
- 아침 묶음에서 운세의 위치(레슨 뒤) — plan-fortune-daily (가) 결정 유지.
- ~~`_build-beginner` / `_build-intermediate` 의 내용 개정~~ — **2026-08-08 완료** (§5-B).
- `quiz-*-review.json` 3파일 정리 — 신양식 시드 후 불용이 되지만 지우지 않는다.
- system-overview.txt 의 렌더 절 갱신 — 후속 문서 정비로.
- 중급 커리큘럼·리치메뉴·결제 (§8).

## 4. 진행 체크리스트

- [x] §1 render.mjs 신양식 + 호환 + 꼬리통
- [x] §1-3 fortune-text bridge 절 제거 + push-daily 배선
- [x] §4 verify-render(67) / verify-push(58) / verify-fortune-server(20) / verify-quiz(19)
- [x] 관문 19종 통과 → 커밋① `82fe964` (§1+§4) ※ verify-server 1건은 루트의 무관한
      package.json(§6 참고) 탓 — CI에는 없는 파일이라 CI는 통과
- [x] §2 content-check 7규칙 + 관문 → 커밋② `bb5529d`
- [x] §3-1 _build-lib 공통 + §3-2 교체 확정·반영 → 본 문서 §5
- [x] 상급 101일 신양식 생성 → `seed-content --check` 303일 전수 0건 통과
- [x] **초급 101일 · 중급 101일 신양식 재작성** → 303일 전부 신양식 (§5-B)
- [x] §7 완료 보고 → 본 문서 §7

## 5. §3-2 교체 — ★ 확정·반영 완료 (2026-08-08)

대표 확정: **추천안 그대로**. 반영 후 재생성해 중복이 사라진 것을 실측했다.

| 자리 | 구(문제) | 신(확정) | 확인 |
|---|---|---|---|
| **84일** | `-고자 하다` — 4일 `-고자` 와 계열 중복 + 뜻풀이 오역 | **`-노라`**（〜するぞ ※고전 자칭 종결） | 4일과 중복 해소. 76〜78 고전 종결 계열을 회수 |
| **39일** | `-기가 이를 데 없이` — 37일의 부사 활용형 | **`-(으)ㅁ을 금할 수 없다`**（〜を禁じ得ない） | 2일차 `-(으)ㅁ` 회수 구조 |
| **41일** | `-기 그지없다` — 37·33 과 동의어 | **`-(으)ㄴ/는 마당에`**（〜という状況で） | 의미장 분산, 42〜44 앞의 상황 제시 |

교체 후 「이 상 없음」 군집은 **33·34·37 세 개**만 남았다 — 각각 `-기 짝이 없다`(부의 극치)·
`-(으)ㄹ 나위가 없다`(정의 극치)·`-기 이를 데 없다`(사전 표제형)로 형태와 용법이 갈리는
별개 항목이라 존치가 맞다.

### 5-1 지시서 확정분 3건 (이미 반영)

| 위치 | 현행 결함 | 반영 |
|---|---|---|
| 65일 | `-을 수반되다`（비문·피동이 목적어 받음） | `-을/를 수반하다`. tip 에 「수동은 -이/가 수반되다」 대비 명시 |
| 14일 | 뜻풀이·퀴즈 2곳에 중국어 `不顾` | `押して・かえりみず` 로. 신양식 quiz 는 autoQuiz 생성이라 「不顾」자체가 소멸 |
| 전일 | bridge 가 전일 동일문(`FB()`) + 「運がよい」단정 | **303일 전부 고유 문면**으로 신작. 운세 단정 0 (§2 규칙6) |

<details><summary>확정 전 제시했던 후보 A/B/C (이력)</summary>

84일: A `-노라` / B `-(으)ㄹ까 보냐` / C `-(으)ㄴ즉` ·
39일: A `-(으)ㅁ을 금할 수 없다` / B `-(으)ㄹ 노릇이다` / C `-(으)ㄹ 지경이다` ·
41일: A `-(으)ㄴ/는 마당에` / B `-(으)ㄹ 판이다` / C `-(으)ㄹ 노릇이다`.
세 후보 모두 303일 + `curriculum.md` 전체 미사용 확인. **A 안으로 확정.**
</details>

## 5-B. 초·중급 신양식 재작성 (2026-08-08 완료)

지시서 §3-3 순서 2〜4를 이어서 수행했다. **303일 전부 신양식**이 되었다.

| 코스 | 범위 | 방식 |
|---|---|---|
| **초급** | 1〜101 | 1〜15는 먼저 신양식으로 옮겨져 있던 문면을 그대로 계승, 16〜101 신작. 문법 배열은 기존 커리큘럼 순서 유지(배달 중 구간이라 진도가 어긋나지 않게) |
| **중급** | 1〜101 | 전량 신작. 간접화법(1〜10) → 원인·이유(11〜25) → 피동·사동(26〜35) → 대조·양보(36〜50) → 추측·비유(51〜65) → 한정·비교(66〜80) → 습관·화제·강조(81〜101) |
| **상급** | 1〜101 | §3-2 교체 3건 반영 후 재생성 |

### 5-B-1 작업 중 걸린 것 (관문이 잡아 준 것)

- **`fb` 불일치 4건** — 마지막 대사가 그날 형태로 끝나는지 보는 빌드 검사(§3-1)가
  조사 축약(`를 기점으로` vs 실문 `달을 기점으로`) 4건을 잡았다. 전 일수를 모아
  한 번에 보고하도록 고쳐 왕복을 줄였다.
- **오답 풀 고갈 2건** — 뜻 겹침 판정이 일본어 기능 형태소(`する`·`ない`)까지
  내용어로 세어 후보를 과잉 배제했다. 기능 형태소 불용어를 두어 해소
  (`この上`·`なく` 같은 진짜 의미 군집은 그대로 걸린다). 중급 2일차는 앞선 날이
  1일뿐이라 구조적으로 풀이 안 나와 **손으로 쓴 퀴즈**를 넣었다.
- **가운뎃점 U+00B7 / U+30FB** — 둘 다 `ODD_CHAR` 허용 밖(U+30FB 는 Script=Common
  이라 Katakana 로 통과하지 못한다). 검사 대상은 대화 `kr` 과 `bridge.kr` 뿐이라
  해당 필드에서만 제거했다.
- **화살표 U+2192** — 불규칙 활용 설명(`듣다 → 들어요`)을 bridge 에 쓰면 걸린다.
  tip·grammar_point 는 검사 대상이 아니라 그대로 두고, bridge 문면만 풀어 썼다.
- **tip 을 한국어로 쓴 것(202일)** — 관문이 잡지 못한 유일한 것. 대표 지적으로 발견해
  전량 일본어로 재작성했다(§1-4c). **입고 검사는 tip 의 「形/使/落 3행 정형」만 보고
  언어는 보지 않는다** — 자동 검출을 원하면 규칙을 추가해야 한다(현재 범위 밖).

## 6. 작업 중 발견 (무관·보고용)

- 저장소 루트에 무관한 `package.json`/`package-lock.json`/`node_modules`
  (untracked, `@google/design.md` 라는 패키지, 2026-08-08 01:02 생성)가 있어
  `verify-server` 의 「ルートに package.json は無いまま」 1건이 로컬에서만
  실패한다. 커밋되지 않은 파일이라 CI·배포에는 영향 없음. 지우지 않고 보고만.

## 7. 완료 보고 (지시서 §7)

### 7-1 커밋 해시 — §1 / §2 / §3 별

| 지시서 | 커밋 | 내용 |
|---|---|---|
| §1 렌더러 + §4 렌더 관문 | **`82fe964`** | render 신양식·구양식 호환·꼬리통 / fortune-text bridge 절 제거 / push-daily 배선 / verify-render·push·fortune-server |
| §2 입고 검사 + 관문 | **`bb5529d`** | content-check 신양식 7규칙 / verify-render §2절·verify-quiz |
| §3 생성 스크립트·원고 | **미커밋(gitignore)** | `_build-lib.mjs`·`_build-{beginner,intermediate,advanced}.mjs`·303일 JSON 은 `server/content/`(유료물) 안. 서버 반영은 D1c CI(private repo→FTPS→deploy→seed) |

배포①(`82fe964`)는 `deploy-server` + `Deploy to Xserver` CI **둘 다 success**.
배포②(`bb5529d`)는 push 완료(§7-7).

### 7-2 신·구 양식 1일치 실렌더 (dry-run 전문)

**신양식 · 상급 51일**(실물 생성물 `advanced-46-60.json`, 이용자 「켄」):

```
[1통] 📘 Day 51 : -(이)라기보다 / [〜というよりは]
      🔗 接続 (活用ルール) / 名詞 + (이)라기보다。パッチム有 → 이라기보다／無 → 라기보다
      💡 学習ポイント / ・使: 一度貼られた名札を貼り替えます。「선물이라기보다 편지」
                        ・落: 比較の -보다 と違い、比べるのは物差しではなく呼び名です
      💬 実際に使ってみよう / 友だち：〔상자를 열며〕 이거, 생일 선물이에요?
                              （〔箱を開けながら〕これ、誕生日プレゼントですか。）
                            けん：네. 켄은 안에 손편지도 넣어 뒀어요.
                              （はい。けんは中に手書きの手紙も入れておきました。）
                            友だち：감동이에요. 선물이라기보다 마음이네요.
                              （感動です。プレゼントというより気持ちですね。）
[2통] 📚 今日の単語 / 【名詞】 1. 선물 プレゼント 2. 손편지 手書きの手紙
                    【形容詞】3. 감동적이다 感動的だ 4. 정성스럽다 真心がこもっている
                    【動詞】 5. 담다 込める 6. 열다 開ける
[3통·꼬리] ❓ 今日のクイズ / 「〜というよりは」に当たる形は？
           ① 라기보다 ② 차치하고 ③ 하였으나
           🍀 今日のひとこと / 勉強というより習慣。呼び名を変えれば負担も減ります。
           [quickReply] ①②③ → data=action=review&day=51&choice=N
```

**구양식 · 초급 4일**(기존 시드분 `beginner-01-15.json`, pos 없음):

```
[1통] 📘 Day 4 : -은 / -는 / [〜は ※主題・対照]
      💡 学習ポイント / 주제를 말할 때 씁니다. 받침 있으면 -은, 없으면 -는. 「저는 학생이에요」
        ← 形/使/落 없으므로 분해 안 하고 💡 아래 통째. 🔗 섹션 없음
      💬 実際に使ってみよう / 친구：취미가 뭐예요? …
[2통] 📚 今日の単語 / ・취미 趣味 / ・음악 音楽 / ・은/는 〜は
        ← pos 없으므로 평면 목록（【名詞】 묶음 없음）
      🍀 今日のひとこと / 今日の学習運がよいです。「은는」を…
        ← quiz 없어 ❓ 생략, bridge 는 2통 말미로 이동. throw 없음
```

두 케이스 모두 **헤더 7종은 코드가 붙인 것**(원고 JSON 에 이모지 0).
구양식은 헤더가 붙고 bridge 위치만 이동 — §6-1 이 못박은 배포① 화면 변화 그대로.

### 7-3 bridge 이중 출력 없음 (운세 메시지 diff)

`fortune-text.mjs` 의 `fortuneMessage` 에서 **bridge 절(4줄)만** 제거:

```diff
-  if (bridge && bridge.kr) {
-    out.push("", "──────────", bridge.kr);
-    if (bridge.ja) out.push(bridge.ja);
-  }
-
   return { type: "text", text: out.join("\n") };
```

`{ bridge = null }` 인자와 push-daily 의 전달부(`fortuneSection(u, tpl, …)` →
`fortuneSection(u, …)`)도 제거. 운세 메시지의 1〜4절(총운·십신·고저·나머지)은
한 글자도 안 바뀜 — `verify-fortune-server` **19→20**(신규 1건이 「bridge 식별자
0건·구분선 없음」을 소스·출력으로 확인). `verify-push` 에 「레슨에 1회·운세에
0회」 관문 신설. 지시서 §4 의 「fortune-server 17항목 흔들림」 예상과 달리
**19항목 무변화**(병행 조사 §3-1 이 지적한 대로).

### 7-4 §2 7규칙 — 깨뜨린 입력이 걸린 로그 (`verify-render` §2절)

```
✓ 규칙 1: tip 에 形・使・落 가 다 없으면 거부 (落 누락이 걸림)
✓ 규칙 2: 대화가 3행이 아니면 거부 (2행·4행 다 걸림)
✓ 규칙 3: vocab 6어・pos 필수・품사별 2개 (5어 / 副詞 / 2·1·3 전부 걸림)
✓ 규칙 4: 신양식은 quiz 필수 (누락이 걸림)
✓ 규칙 5: 섹션 헤더 이모지가 원고에 있으면 欄 이름으로 거부 (tip/bridge/quiz)
✓ 규칙 6: bridge 의 운세 단정 (kr·ja 양쪽 — 運がよ/운이 좋)
✓ 규칙 7: 구양식은 기존 규칙만 (3어·운세단정 bridge 그대로 통과 — 이행기 재시드)
✓ 온전한 신양식 1일치는 0건 (거짓 양성 없음)
```

`verify-quiz` +1: 「신양식(pos 있음)에서 quiz 누락 거부」.

### 7-5 §3-2 교체 → **§5** (확정·반영 완료)

84 `-노라` · 39 `-(으)ㅁ을 금할 수 없다` · 41 `-(으)ㄴ/는 마당에` 로 확정·반영.
교체 후 중복 군집은 33·34·37(별개 항목) 세 개만 남았고, 4↔84 계열 중복은 소멸.
이어서 **초급·중급 101일씩도 신양식으로 재작성**해 303일 전부 신양식이 되었다(§5-B).

### 7-6 1통/2통 분할 결정 (§1-1)

- **1통** = 문법(📘)+접속(🔗)+학습포인트(💡)+회화(💬), **2통** = 단어(📚) — 현행 분할 유지.
- **꼬리통** = 퀴즈(❓)+한마디(🍀). push-daily 가 뽑아 **운세·부적·예고 뒤(묶음 맨 끝)** 로.
  근거: quickReply 는 묶음의 **마지막 1통에서만** 열린다(절목 퀴즈가 말미로 가는
  기존 이유). ❓를 2통 안에 두면 운세가 뒤에 오는 순간 답 버튼이 화면에서 사라진다.
- 통수 상한(LINE 5): 보통·예고·절목 전 조합 **5통 이내**. 부적의 `<5` 판정에
  꼬리통을 셈에 포함. 예고·절목 아침엔 데일리 ❓를 접어(quizSection:false) 재구매
  버튼/기록 퀴즈에 말미를 양보.

### 7-7 관문 총계

지시서의 「678」은 저장소 어디에서도 재현되지 않는다(병행 조사 §3-4-a 도 동일).
실행 계수로 대체한다. **이번에 바뀐 4관문:**

| 관문 | 이전 | 이후 | Δ |
|---|---|---|---|
| verify-render | 50 | **67** | +17 |
| verify-push | 56 | **58** | +2 |
| verify-fortune-server | 19 | **20** | +1 |
| verify-quiz | 18 | **19** | +1 |

19종 중 **17종 PASS**. FAIL 2종은 **둘 다 이번 작업과 무관**하다:

| 관문 | 사유 |
|---|---|
| `verify-server` 1항목 | §6 의 루트 무관 `package.json`(untracked) — CI·배포 무영향 |
| `verify-onboarding` 1항목 | 작업 트리에 **커밋되지 않은 `index.html` 수정**이 있다(제3자 작업: 出生地 → 生まれた場所 등 문면 정리). 관문은 `describeLinkPayload` 의 `shown` 에 `出生地` 가 있는지를 본다. 원고 작업은 `index.html` 을 건드리지 않았다 — 해당 수정의 마무리(관문 문구 또는 페이지 문면 중 하나를 맞추는 일)가 남아 있다 |

`seed-content --check` 는 **303일 전수 0건**(초·중·상급 각 101일, 전부 신양식·quiz 101·bridge 101).

### 7-8 남은 것 (다음 지시 대기)

- **서버 반영(시드)** — 원고는 로컬 `server/content/` 에만 있다. 실제 배달에
  반영하려면 private repo 로 push 해 D1c CI(FTPS→deploy→seed)를 태워야 한다.
  **저녁 6시 이후 그날 원고 재배치 금지**(STATUS §8-9) 규칙은 그대로.
- **저녁 복습의 신양식 대응** — 지시서 범위 밖(§3). 저녁 답변에는 tip 3행·단어
  6개가 원문 그대로 나가며 깨지지는 않는다. 스타일을 맞추려면 별도 지시가 필요.
- **초급 자동 퀴즈의 난도** — 오답을 「직전 1~7일의 문법 형태」에서 뽑는 규칙
  (§3-1)을 지키다 보니, 초급처럼 인접일의 문법 범주가 다른 구간에서는 보기가
  쉬워지는 날이 있다(조사 3개 중 어미 1개 등). 규칙대로의 결과이며, 손으로 쓴
  퀴즈는 1〜15·30·50·75·101일에 넣어 두었다.
