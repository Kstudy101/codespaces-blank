# 라이브 검증 지시서 — 복습 퀴즈 (E1)

> STATUS: **E1** — [STATUS.md](../STATUS.md) §0.
> 코드·관문 통과 (2026-08-04). **D3 초급 1〜50 `quiz` 백필** 후 실사용자 주기로 확인.

작성: 2026-08-08 / 근거: [plan-quiz.md](plan-quiz.md)

## 전제

- `push-daily.mjs` 배포 완료 (복습·절목 분리, 절목일·기한예고일 스킵)
- **3코스** day 1〜50 + 절목 75 각 **51문** (`quiz IS NOT NULL`) — 로컬 `--check` ✓
- 합계 **153문** (복습 pool 48일×3 + 절목 30/50/75×3)
- 서버 DB 반영: **D1c** (private repo → FTPS → deploy → seed). D0 수동 경로는 사용하지 않음
- 테스트 계정으로 통로 검증은 **2026-08-05 완료** (`push-daily --user=<id>`)

## §1. 발송 조건 (3의 배수 아침)

| # | 조건 | 기대 |
|---|---|---|
| 1 | `current_day + 1` 이 3의 배수 (3·6·9…) | 아침 본편 **뒤**에 복습 퀴즈 1통 |
| 2 | 같은 날이 절목(30/50/75) | 복습 퀴즈 **없음** (절목 퀴즈만 — E2) |
| 3 | 같은 날 기한예고(`remaining=2`) | 복습 퀴즈 **없음** (예고 우선) |
| 4 | 3의 배수가 아닌 날 | 복습 퀴즈 없음 |

## §2. 문면·동작

| # | 확인 | 기대 |
|---|---|---|
| 1 | 머리 | `🔁 復習クイズ（N日目より）` + 질문 |
| 2 | quickReply | ①〜④ 선택지, `data=action=review&day=N&choice=i` |
| 3 | data에 정답 없음 | `answer` 필드가 postback data에 **없음** |
| 4 | 정답 탭 | `⭕ 正解です！🎉` 1통 |
| 5 | 오답 탭 | `❌ 残念…　正解は ②…` 1통 |
| 6 | 미학습일 자칭 (`day` > `current_day`) | reply 없음 (skipped) |

## §3. DB (채점 직후)

```sql
-- quiz_pass_log 는 변하지 않아야 함（복습은 무저장）
SELECT quiz_pass_log FROM learning_progress WHERE user_id = <ID>;
```

복습 퀴즈 전후 **동일**.

## §4. 실사용자 주기 확인

| # | 방법 | 기대 |
|---|---|---|
| 1 | 초급 수강 중인 실계정이 3·6·9…일차 아침 | §1〜§2 통과 |
| 2 | `push_logs` | `push_type='learning'` 만. 복습은 learning 배치에 동봉 (별도 quiz 행 없음) |

## §5. 보고

- §1〜§4 통과/실패 · 실패 시 캡처·일차
- 서버 seed 후 `quiz` 건수: **코스당 51문 · 합계 153문** 목표
- 배포 커밋 해시
