# plan-follow-silence-live — ★-LIVE 추적

작성: 2026-08-09. 상태: **해결（대표 확인）**

## 결과
**ブロック → 再追加**（아무 말도 치지 않음）만으로  
`welcomeBoard` + 読み方質問이 도착. = `handleFollow` 정상.

OA あいさつはオフのまま（二重防止）。サーバ正本 `aacf6dc`。

## 경과（짧게）
| 시각 | 관찰 | 판정 |
|---|---|---|
| 배포 직후 | 재추가해도 침묵 | follow 미발생으로 추정 |
| `あ` 입력 | 보드+読み方 | `!user` recover 안전망 |
| ブロック→再追加 | 말 없이 동일 화면 | **follow 정상 · ★-LIVE 통과** |

reply/push 분리안은 불필요 → 구현하지 않음.

## 운용 메모
친구추가 인사말이 안 나올 때는 먼저 **本当にブロックしたか** を疑う。
非表示・トーク削除だけでは follow が来ない。
